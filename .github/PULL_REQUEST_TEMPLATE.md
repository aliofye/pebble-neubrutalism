## Summary

<!-- What does this change and why? -->

## How I tested

<!-- JS: `npm test`. Pure C: `./scripts/test-c.sh`. Watch-side: which QA-checklist steps you ran (emulator/device). -->

## Checklist
- [ ] `npm test` passes
- [ ] `./scripts/test-c.sh` passes (if pure C helpers changed)
- [ ] `pebble build` succeeds (if watch code changed)
- [ ] Added/updated tests for JS and pure-C changes
- [ ] Ran the relevant items in `docs/QA-checklist.md` for watch-side changes
